import { ClientLocation, ThemeOptions } from "../types"

/** default set to phila, pa */
export const defaultLocation: ClientLocation = {
    latitude: 39.96187,
    longitude: -75.155365,
    timezone: 'EST'
}

export const defaultTheme: ThemeOptions = 'LIGHT';
